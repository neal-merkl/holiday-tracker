<h1>Add new Holiday</h1>
<div>
  <%= form_for(@holiday) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :date %>
    <%= f.text_field :date %>

    <%= f.submit "Add new Holiday", class: "btn btn-primary" %>
  <% end %>
</div>
